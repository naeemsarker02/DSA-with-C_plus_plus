int n;
    cin >> n;
    int arr[n];
    // Take input
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    // Print array
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }